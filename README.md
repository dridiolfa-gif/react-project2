# React Project 2

This project was created with a Create React App-style structure.

## Available Scripts

In the project directory, you can run:

- `npm start`
- `npm run build`
- `npm test`

## Included Files

- `src/App.js` as the root component
- `src/product.js` exporting a product object with name, price, description, and image
